# 5.1 - Calendar views (page shell + week/month/day grids)
LAYER: L5-calendar / TIER: heavy / RISK: high

GOAL: Rebuild the Calendar space (pages/Calendar.tsx and the WeekGrid week spread)
on Lynx elements so week and month views render seeded dated tasks in the right
cells, statically (no drag gesture -- that is unit 5.2's job).

CONTEXT (what exists when this unit starts):
- L1-L4 are complete and E2E-green through @l4. The new ReactLynx tree lives in
  src/ (rspeedy defaults); the old React-DOM app is READ-ONLY reference at
  src-legacy/ (unit 1.1 did `git mv src src-legacy`).
- Unit 1.2 already ported the pure-logic module src-legacy/features/tasks/timeGrid.ts
  verbatim to src/features/tasks/timeGrid.ts. Do NOT re-port it. It exports:
  DAY_START (=360), DAY_END (=1380), SNAP (=15), GUTTER (=48), PX_MIN, PX_MAX,
  PX_DEFAULT, clampPx, PxPerMinProvider, usePxPerMin, clamp, snap, fmtMin,
  layoutLanes. Unit 1.2 also ported src/features/tasks/dates.ts (localDate,
  dateOnly, toPbDate, toLocalNoon, addDays, mondayOf, weekOf, dayTitle, dueInfo)
  and src/lib/date.ts (pad2, nextMonth).
- Unit 1.3 ported src/features/tasks/api.ts (useDayTasks, useMonthOpenCounts,
  useProjectTasks, useTasksLive, useUpdateTask, ...). Do NOT re-port it.
- Unit 4.1 (Today) ported PlannerBook (its binding constants) but DEFERRED the
  WeekStrip / MonthView date shelf AND the whole view/month/select state TO THIS
  UNIT (verified: 4.1 story PARKED list "The WeekStrip/MonthView date shelf and
  month project-dots (unit 5.1)" + its DEFER-to-5.1 note). So WeekStrip.tsx and
  MonthView.tsx are NOT in the new tree yet -- THIS UNIT ports them, and THIS UNIT
  restores them into Today.tsx (which 4.1 rendered shelf-less). Unit 4.3 ported the
  AgendaSheet + ComposerSheet + the TimeboxSheet UI (static day sheet).
- The month project-dots hook useMonthProjectDots lives in features/learning
  (depends on useLearningStore + categories) and is NOT ported until L6 (learning),
  which runs AFTER this layer. So at 5.1 runtime it does NOT exist in the new tree.
  Every MonthView mount here passes `sessionDots={}` (empty). This is correct, not a
  stub: no learning programs exist until L6, so project-dots are empty anyway. L6
  (unit 6.1/6.3) rewires the real hook into Today.tsx + Calendar.tsx. Leave a
  documented seam comment at each call site (`// L6: sessionDots={useMonthProjectDots(month)}`).
- The design system (Panel, Eyebrow, Masthead, safe-area, tokens, fonts, icons)
  landed in L2/L3; the router + dock + guard landed in L3. The `/calendar` route
  and its guard already exist (L3 router). This unit fills in the page component.
- calendar_events (Google events) rendering is NOT part of this unit; it is unit
  5.3. This unit renders tasks only.

IN SCOPE (files):
- CREATE  src/pages/Calendar.tsx        (port of src-legacy/pages/Calendar.tsx)
- CREATE  src/features/tasks/components/WeekGrid.tsx
                                         (port of src-legacy/.../WeekGrid.tsx, RENDER ONLY)
- CREATE  src/features/tasks/components/WeekStrip.tsx  (port; 4.1 deferred it here)
- CREATE  src/features/tasks/components/MonthView.tsx  (port; 4.1 deferred it here)
- MODIFY  src/features/tasks/index.ts    (add WeekGrid, WeekStrip, MonthView exports)
- MODIFY  src/pages/Today.tsx            (restore the WeekStrip/MonthView date shelf
                                          + view/month/select state that 4.1 deferred
                                          to this unit; pass sessionDots={})
- CONSUME (do not re-create): PlannerBook.tsx, ComposerSheet.tsx, TimeboxSheet.tsx
  (4.x), timeGrid.ts, dates.ts, api.ts (1.x). STEP 0 verifies these exist.
- src-legacy consumed by this unit (git rm at end, record in BROOM):
  src-legacy/pages/Calendar.tsx, src-legacy/features/tasks/components/WeekGrid.tsx,
  src-legacy/features/tasks/components/WeekStrip.tsx,
  src-legacy/features/tasks/components/MonthView.tsx.

OUT OF BOUNDS:
- Any drag/resize/drop gesture on any calendar block. Rendered blocks are static
  Links in this unit. All gesture code is unit 5.2 (see BROOM defer entries).
- calendar_events / Google events (unit 5.3). pb/pb_hooks and pb/pb_migrations
  (never touched by any client unit). pb/pb_data (untouchable, hard rule).
- TimeboxSheet.tsx internals (owned by 4.3 for UI, 5.2 for drag). This unit only
  MOUNTS <TimeboxSheet> in the day view; it does not edit that file.

BROOM (port / drop / defer for every old symbol in the two ported files):
Calendar.tsx:
- PORT: the View type ("day"|"week"|"month"), CalendarBody state machine (view,
  selected, direction, month, week, today, slot), go(), openDay(), weekLabel,
  ViewToggle, NavButton, ZoomStepper, SignedOut. The +/- ZoomStepper stays: it is
  the web-parity zoom control (state-driven px, no gesture).
- DROP `import { AnimatePresence, motion } from "motion/react"` and every
  motion.*/AnimatePresence usage. WHY: PLAN section 8 removes motion; crib says
  CSS transitions/keyframes for enter/exit. Replace the week-swipe and month-drop
  entrance animations with a CSS keyframe/transition (PLAN 5.3 "declarative
  enter/exit -> Lynx CSS transitions + keyframes"). The `today` fade button and the
  ViewToggle `layoutId` sliding pill become: a plain conditional <text> for the
  today button, and a CSS-transitioned highlight <view> for the toggle (no shared-
  element layout animation on Lynx -- approximate with a positioned highlight).
- DROP `usePinchZoom` entirely (the whole hook: touchstart/move/end + wheel, all
  DOM addEventListener + getBoundingClientRect + window.scrollY). WHY: it is a
  native-DOM touch/wheel gesture; Lynx has no window wheel/passive-listener model,
  and the +/- ZoomStepper already gives web parity for zoom. MTS pinch-to-zoom is
  device polish, not required for the @l5 gate -> see PARKED. Remove the
  `ref={pinchRef}` and the `style={{ touchAction: "pan-y" }}` wrapper with it.
- DROP `lucide-react` imports (ChevronLeft/Right, Minus, Plus). WHY: PLAN 5.9 --
  lucide-react is React-DOM SVG. Use the L2 icon set (src/components/icons, ported
  in unit 2.4). If a needed glyph is missing there, STOP-on-ambiguity (do not
  invent an icon).
- PORT-ADAPT: `<Link>` from @tanstack/react-router stays (router is memory-history
  from L3; Link works). `@/components/masthead`, `@/components/surface`
  (Panel/Eyebrow), `@/features/style` PageDoodle, `@/stores` useAuthStore -- all
  from earlier layers; import as-is.
WeekGrid.tsx:
- PORT: WeekGrid render (days, DayHeader, HourRules mount, the vertical day-divider
  <view>s, the column layout), WeekColumn, WeekSessionBlock, WeekBlock (as a static
  positioned Link -- see DEFER), layoutLanes usage, SOLO_LANE, EMPTY_EXTERNS.
- DROP `import { motion } from "motion/react"`; WeekBlock/ghost motion.* usage.
- DEFER-TO-5.2: WeekBlock's drag (drag, onDragStart/onDrag/onDragEnd,
  onClickCapture, whileDrag, dragging ref), the ghost preview state + <view>,
  resolvePoint(), preview(), drop(), and the getBoundingClientRect/window.scroll
  math they use. In THIS unit WeekBlock renders as a static positioned <view>/Link
  at top=(start_min-DAY_START)*px, height=dur_min*px, in its lane -- no drag props.
  Leave a clearly-commented seam (e.g. `// 5.2: MTS drag mounts here`) so 5.2 has a
  precise attach point. Keep the tap-to-open-day (DayHeader onPick) and
  tap-empty-to-add catcher (onAddSlot) -- those are taps, not drags (see SPEC 6).
- DROP `externsByDay`/`externs`/AgendaExternal/WeekSessionBlock-from-externs wiring
  as DEAD input in this unit (no producer exists; learning sessions are real tasks
  now). BUT keep the `externsByDay` prop and WeekSessionBlock component in the file
  as an inert, empty-defaulted seam -- unit 5.3 revives it to render Google events.
  Record: "externsByDay retained as an empty-default seam for 5.3."
WeekStrip.tsx:
- PORT: the seven day-chip ribbon, weekAnchor state, day paging (addDays +/-7),
  monthLabel, todayInView, "back to today" button, PagerButton, weekday-narrow +
  date rendering, today-dot.
- DROP `import { motion, AnimatePresence } from "motion/react"` and all motion.*
  usage. The layoutId="week-selected" sliding highlight -> a CSS-transitioned
  highlight `<view>` (or an instant swap if left/width transitions don't hold on
  Lynx). whileTap scale -> the preset uiVariants pressed state or a CSS active
  transition. The "back to today" AnimatePresence -> conditional mount + a CSS
  fade-in class.
- DROP `lucide-react` (ChevronDown/Left/Right) -> L2 icon set (unit 2.4).
- PORT-ADAPT: div->view, span->text (explicit color/size on every text).
MonthView.tsx:
- PORT: the month grid math (first, mondayOffset, daysInMonth, weeks, gridStart,
  cells), shiftMonth, WEEKDAYS header, per-day cell with task dots (from
  useMonthOpenCounts, a tasks hook available at L5) + session dots (from the
  sessionDots prop -- passed {} at L5), selected/today styling, HeaderButton.
- DROP `motion` (layoutId="month-selected" sliding highlight + whileTap) -> same
  CSS-highlight treatment as WeekStrip. DROP `lucide-react` -> L2 icon set.
- PORT-ADAPT: keep the `sessionDots` prop; callers pass {} at L5 (CONTEXT). div->
  view, span->text (explicit color/size).

SPEC (numbered; concrete; cite crib + docs):
0. VERIFY prerequisites in the NEW tree (do not assume). Run these greps; act on
   results, do not improvise:
     grep -rl "export function PlannerBook" src/features/tasks/components/
     grep -rl "export function AgendaSheet" src/features/tasks/components/
     grep -rl "export function ComposerSheet" src/features/tasks/components/
     grep -rl "export function TimeboxSheet" src/features/tasks/components/
     grep -rn "usePxPerMin\|PxPerMinProvider\|PX_DEFAULT\|clampPx\|layoutLanes" src/features/tasks/timeGrid.ts
     grep -rn "useDayTasks\|useMonthOpenCounts\|useTasksLive" src/features/tasks/api.ts
     grep -rn "scroll-view\|scroll-orientation" src/  (find the L3 page scroller)
   - PlannerBook/AgendaSheet/ComposerSheet/TimeboxSheet present (4.x) -> import them,
     do NOT re-port. Absent -> STOP-on-ambiguity (they are prerequisites from L4).
   - timeGrid/api symbols present (1.x) -> use them. Absent -> STOP.
   - WeekStrip/MonthView are EXPECTED ABSENT (4.1 deferred them here). Port them in
     this unit -- do NOT stop on their absence. Do NOT import useMonthProjectDots
     (it is L6/learning, not ported yet) -- pass sessionDots={} (see CONTEXT).
1. ELEMENTS, not HTML (crib "Elements, not HTML"). Rewrite every div->`<view>`,
   span/p/h2->`<text>`, button->`<view bindtap=...>` (or the L2 Button primitive).
   TEXT DOES NOT INHERIT CSS (crib): every `<text>` sets its own color/
   font-family/size via the existing Tailwind token classes (text-ink,
   text-ink-muted, text-zest, font-display, the [11px]/[9px] sizes). Verify each
   ported `<text>` carries an explicit color + size class; a bare `<text>` inherits
   nothing on Lynx. Ref: crib sheet; https://lynxjs.org/rspeedy/styling .
2. SCROLLING (crib "<scroll-view>/<list>"). Enumerate exactly how each view
   scrolls, because Lynx `<view>` does NOT scroll:
   - WeekStrip: a fixed 7-chip row -> `<view>` (no scroll).
   - MonthView: a static 6x7 grid -> `<view>` (no scroll).
   - Week spread + day sheet: a tall absolutely-positioned canvas whose height is
     (DAY_END-DAY_START)*px (>= 1020px at PX_DEFAULT) -> it must scroll vertically.
     Prefer the L3 shell's page-level `<scroll-view>` (STEP 0 locates it): the whole
     Calendar page scrolls, exactly like the old app scrolled the window. If the
     shell does NOT wrap page content in a vertical scroller, wrap the day/week
     canvas in `<scroll-view scroll-orientation="vertical">`. Do NOT use `<list>`:
     it recycles items and virtualizes, which breaks the absolute top/height
     positioning the grid relies on; `<scroll-view>` renders all children. Ref:
     crib sheet; https://lynxjs.org/rspeedy/styling .
3. PAGE COMPONENT src/pages/Calendar.tsx. Keep the same shape: <Masthead ...>,
   isAuthenticated ? <CalendarBody/> : <SignedOut/>. Preserve all state, go(),
   openDay(), weekLabel. Wrap the day/week region in <PxPerMinProvider value={px}>
   (from timeGrid). Keep the day view mounting <PlannerBook page={selected}
   direction={direction}><TimeboxSheet date={selected} onAddSlot={addAt}/></...>,
   the week view mounting <Panel><WeekGrid anchor={selected} onPickDay={openDay}
   onAddSlot={addAt}/></Panel>, and the month view mounting <Panel><MonthView .../>.
   Keep the ComposerSheet slot drawer: on slot state set, mount <ComposerSheet
   date=... initialStart=... onClose=.../>.
4. ANIMATION (crib "Animation"; PLAN 5.3). Replace motion entrances with CSS:
   - week swap and month drop: a CSS keyframe (fade + small translate) applied via
     a Tailwind/utility class keyed on `view`/`week[0]` (remount triggers the
     keyframe). Keep durations within CLAUDE.md discipline (page transitions
     <= 350ms; interactions <= 200ms).
   - ViewToggle active pill: a `<view>` highlight positioned under the active
     segment with a CSS `transition` on left/width (no motion layoutId; Lynx has no
     shared-element layout animation -- confirm and, if transitions on left/width
     are unsupported for this layout, fall back to instantly swapping the
     highlight, which is acceptable for the @l5 gate). Ref:
     https://lynxjs.org/react/main-thread-script (gesture motion is 5.2; this is
     declarative only).
5. ZOOM: keep px as React state; ZoomStepper In/Out call setPx((p)=>clampPx(p*1.4))
   / (p/1.4). Buttons are `<view bindtap=...>` with explicit `<text>`-free icon
   children from the L2 icon set. Disabled state via the preset uiVariants or a
   conditional class (crib: no pseudo/data-attr selectors -- do not rely on
   :disabled). The stepper is position:fixed above the dock; keep its z-index above
   page content.
6. TAP-TO-ADD and TAP-TO-OPEN (taps, not drags -- allowed here):
   - DayHeader onPick -> openDay(day): a `<view bindtap>`.
   - The empty-grid add catcher: the old code computed the minute from
     e.clientY + getBoundingClientRect. On Lynx, read the tap's ELEMENT-relative y
     from the touch event: the catcher `<view>` fills the grid, so
     event.detail?.y (or event.touches[0].y -- the element-coordinate value per the
     TouchEvent docs) is pixels from the catcher's top. Compute
     min = clamp(snap(DAY_START + y / pxPerMin), DAY_START, DAY_END - SNAP) and call
     onAddSlot(day, min) (week: also derive the column from x /
     (columnWidth)). This uses NO boundingClientRect (element-relative coords make
     it unnecessary). If element-relative y is not delivered on tap in the web
     target, STOP-on-ambiguity rather than guessing. Refs: touch coords
     https://lynxjs.org/api/lynx-api/event/touch-event ; crib.
7. WeekGrid RENDER-ONLY per BROOM: blocks are static positioned Links; keep
   layoutLanes lane math, HourRules mount, day dividers. Leave the 5.2 seam comment.
8. RESTORE THE TODAY DATE SHELF (4.1 deferred it here). In src/pages/Today.tsx,
   add back the view("week"|"month")/month/select state and render the shelf above
   the planner pad, exactly as the old Today did: <WeekStrip selected onSelect
   onToggleView={->month}/> or <MonthView month onMonth selected onSelect
   onToggleView sessionDots={{}}/>. Match the old animation-container shape with a
   CSS transition (not motion). Pass sessionDots={} and drop the empty comment seam
   `// L6: sessionDots={useMonthProjectDots(month)}` at the call site. Do NOT import
   useMonthProjectDots (L6). This step makes WeekStrip/MonthView live in BOTH pages.
9. LINT/DEAD-CODE (CLAUDE.md "No dead code"): remove every now-unused import
   (motion, lucide, the pinch hook). No commented-out code, no TODO/FIXME.

DONE MEANS (mechanical gate; run from the worktree root, non-interactive):
1. `npm run typecheck`  -> passes (tsc --noEmit over the new tree + e2e/).
2. `npm run build`      -> passes (rspeedy web output emitted).
3. `npm run lint`       -> passes (no unused imports / dead code).
4. Serve the DISPOSABLE PocketBase (never pb/pb_data):
   `pb/pocketbase.exe serve --http 127.0.0.1:8091 --dir .scratch/pb-e2e/pb_data \
     --migrationsDir pb/pb_migrations --hooksDir pb/pb_hooks`
5. ADD to e2e/ a spec file `e2e/calendar-views.spec.ts` with tests tagged @l5.
   Seeding (as the test app user via the SDK; credentials from
   .scratch/pb-e2e/creds.json per unit 1.5 -- never inline creds): create a task
   with due_date on a known day of the current week, start_min=600 (10:00),
   dur_min=60, title "L5 seed block". Set the Playwright context timezoneId to a
   fixed zone (e.g. "UTC") and the test user's `timezone` field to match, so the
   local day/minute math is deterministic.
   Named @l5 tests:
   - "week view renders a seeded dated task in the correct day column and time slot"
     : open /calendar, switch to week, assert a block with text "L5 seed block"
     exists positioned in the seeded day's column with top ~= (600-360)*px.
   - "month view shows the seeded task's day with an open-task dot": switch to
     month, assert the seeded day's cell shows >=1 zest dot.
   - "day view mounts the timebox sheet for the selected day": open a day, assert
     the seeded block renders in the day sheet.
   - "Today shows the restored week date shelf": open /, assert the seven-day
     WeekStrip ribbon renders (proves the 4.1->5.1 shelf restoration).
6. `npx playwright test --grep @l5`  -> the new specs pass.
7. `npx playwright test --grep "@l1|@l3|@l4"` (or the full suite)  -> all
   previously-green tags STAY green.
8. Commit locally only: `lynx(5.1): calendar views on Lynx elements`. `git rm` the
   consumed src-legacy files listed in IN SCOPE. Never push / PR / tag.

PARKED (cannot verify on this Windows box; write up, never fake):
- Pinch-to-zoom on the time axis (two-finger + trackpad). It was dropped for the
  web target (the +/- ZoomStepper is the web-parity control). A native MTS
  pinch-zoom re-implementation (main-thread:bindtouchstart/move reading
  event.touches[0/1], setStyleProperty on a scale, commit px via runOnBackground)
  is device polish; it needs iOS/Android to verify feel and is out of L5 E2E scope.
- Native-stack drill-in transitions for day open (PLAN 5.2 Sparkling later pass).
