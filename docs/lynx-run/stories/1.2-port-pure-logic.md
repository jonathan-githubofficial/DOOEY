# 1.2 - Port pure-logic modules + all types
LAYER: L1-foundation / TIER: heavy / RISK: low

GOAL: Copy the framework-agnostic logic modules and every types.ts from src-legacy
into the new src/ tree verbatim (broom applied), so later units and the type system
have a stable, DOM-free foundation.

CONTEXT: Unit 1.1 has scaffolded the new src/, the `@/*` alias, tsconfig (excludes
src-legacy), Tailwind, and the `react$ -> @lynx-js/react/compat` alias. src-legacy/
is READ-ONLY reference. No stores, api, pb, or view code exists in the new tree yet
(units 1.3+). This unit adds only pure logic + types.

IN SCOPE (files) - old path (READ from src-legacy) -> new path (WRITE to src):
Pure logic (PLAN 3 list):
  src-legacy/lib/date.ts                 -> src/lib/date.ts
  src-legacy/lib/format.ts               -> src/lib/format.ts
  src-legacy/lib/cn.ts                   -> src/lib/cn.ts
  src-legacy/lib/doodle.ts               -> src/lib/doodle.ts       (broom: drop pointerPct)
  src-legacy/features/tasks/dates.ts     -> src/features/tasks/dates.ts
  src-legacy/features/tasks/timeGrid.ts  -> src/features/tasks/timeGrid.ts
  src-legacy/features/learning/parse.ts  -> src/features/learning/parse.ts
  src-legacy/features/learning/metrics.ts-> src/features/learning/metrics.ts
  src-legacy/features/learning/categories.ts -> src/features/learning/categories.ts
  src-legacy/features/style/tokens.ts    -> src/features/style/tokens.ts
Every types.ts:
  src-legacy/components/page/types.ts    -> src/components/page/types.ts
  src-legacy/features/tasks/types.ts     -> src/features/tasks/types.ts
  src-legacy/features/boards/types.ts    -> src/features/boards/types.ts
  src-legacy/features/learning/types.ts  -> src/features/learning/types.ts

DEFERRED out of this unit (do NOT port here) - see BROOM:
  src-legacy/features/style/backdrop.ts   -> defer-to-unit-1.3 (not pure logic;
    IndexedDB + canvas). The PLAN 3 list names it "drop its localStorage bit", but
    the file has NO localStorage - it is entirely IndexedDB blob storage + <canvas>
    image processing. It is a hard dependency of features/style/store.ts, so it
    ports WITH that store in unit 1.3 (web impl; native blob storage PARKED). 1.2
    does not touch it.
  src-legacy/features/boards/packs.ts     -> defer-to-unit-1.3 (NOT pure logic; it is
    React-Query hooks + pb access. PLAN 3 mislabels it. Its one pure helper,
    normalizeDoodle, ports WITH the hooks in 1.3, which owns features/*/api-style
    modules). 1.2 does not touch packs.ts.

OUT OF BOUNDS: no stores, no api.ts, no pb.ts, no view components, no barrels
(features/*/index.ts). Do not create src/features/style/index.ts etc. Do not import
anything that does not yet exist in the new tree.

BROOM (per file):
- date.ts, format.ts, cn.ts: PORT verbatim. All pure. cn.ts imports clsx +
  tailwind-merge (both KEEP deps per PLAN 8).
- doodle.ts: PORT Stroke, INK_COLORS, InkColor, strokePath verbatim. DROP the
  `pointerPct(e: React.PointerEvent, el: HTMLElement)` export: it is DOM/pointer-
  coupled (getBoundingClientRect) and only consumed by view components that are
  rewritten later (doodle-editor.tsx, FolderStudio.tsx, DoodleTray.tsx). Its Lynx
  replacement (Main-Thread Scripting pointer capture) arrives with the doodle editor
  (unit 3.4) and the board canvas (unit 7.3). Dropping it also removes the file's
  only DOM/React dependency, so the ported doodle.ts is pure and DOM-lib-free.
  Also drop the now-stale comment line referencing pointerPct's renderer if it no
  longer applies; keep the "renderer lives in doodle-svg.tsx" note (doodle-svg is a
  later component).
- tasks/dates.ts: PORT verbatim (imports pad2 from @/lib/date; Intl + Date only).
- tasks/timeGrid.ts: PORT verbatim. It imports createContext/useContext from
  "react" - the 1.1 `react$` alias resolves that to @lynx-js/react/compat, so the
  import string stays "react". Do NOT rewrite it to @lynx-js/react. Pure math +
  React context; no DOM.
- learning/parse.ts, metrics.ts, categories.ts: PORT verbatim. NOTE categories.ts
  imports icon components from lucide-react (LucideIcon type + 5 icons). lucide-react
  is KEEP-pending-validation (PLAN 5.9). It typechecks fine (it is a React component
  lib); whether the icons RENDER on Lynx <svg> is validated at unit 2.4 (icons). For
  1.2 the import only needs to type-resolve, which it does. Do not change it.
- style/tokens.ts: PORT verbatim (pure data + HSL/hex math).
- all types.ts: PORT verbatim. They import only types (`import type { Stroke } from
  "@/lib/doodle"`, cross-type re-exports) - all now resolvable in the new tree.
  boards/types.ts and learning/types.ts also export a few const tables (TEXT_FONT_CLASS,
  STICKERS, FOLDER_FONT_CLASS, DEFAULT_W, ASPECTS, widthOf, ...) - those are pure,
  port with the file.

SPEC (numbered):
1. For each file in IN SCOPE, read the src-legacy original and write an identical
   copy at the new path (create parent dirs as needed). Preserve import strings
   exactly (the `@/...` alias and `react$` alias are already configured by 1.1).
2. Apply the ONE broom edit: in src/lib/doodle.ts remove the `pointerPct` function
   and its `React.PointerEvent`/`HTMLElement` usage. Verify nothing else in the new
   tree imports pointerPct (nothing does at L1 - confirmed: only .tsx view
   components import it, none ported yet).
3. Do NOT create index.ts barrels. Consumers in later units import directly from
   these module paths.
4. Sanity-scan the ported files for DOM/web globals that would break native later
   (document, window, localStorage, indexedDB, getBoundingClientRect): after the
   pointerPct drop there should be ZERO in this unit's files. metrics.ts/dates.ts use
   `new Date()` and Intl (available on both web and PrimJS) - allowed.

DONE MEANS (exact commands + expected outcomes):
  1) All 14 files exist at their new paths; `git status` shows them added under src/.
  2) npm run typecheck  -> exits 0 (the ported files + types resolve; src-legacy still
     excluded).
  3) npm run lint       -> exits 0.
  4) npm run build      -> exits 0 (build unaffected; these are leaf modules).
  5) grep the ported files (src/lib, src/features/*/{dates,timeGrid,parse,metrics,
     categories,types}.ts, src/features/style/tokens.ts, src/components/page/types.ts)
     for `document.|window.|localStorage|indexedDB|getBoundingClientRect|pointerPct`
     -> no matches.
  6) git rm the consumed src-legacy originals for the 14 ported files (broom record:
     ported). Do NOT git rm src-legacy/features/style/backdrop.ts or
     src-legacy/features/boards/packs.ts (owned by 3.4 / 1.3).
  No @l-tag. Commit: "lynx(1.2): port pure-logic modules + all types".

PARKED: none specific to this machine (pure logic verifies fully on web).
