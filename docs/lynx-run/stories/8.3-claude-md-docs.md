# 8.3 - CLAUDE.md + docs update

LAYER / TIER / RISK: L8-cutover / normal (sonnet) / LOW (pure documentation edit,
no code/build risk - but it must be ACCURATE, not aspirational; every claim has
to be grep-verifiable against the real committed tree).

GOAL: Update CLAUDE.md per the PLAN's preamble and Phase 8 checklist (tech-stack
table, kill the Capacitor section, repository layout, UI-components section),
and sweep docs/architecture.md and docs/roadmap.md for statements the migration
made false, so CLAUDE.md and its linked docs describe the Lynx app that actually
exists, not the Vite/Capacitor one.

CONTEXT (what exists when this unit starts):
- L1-L7 are committed and E2E-green through @l7. Units 8.1 (Docker/deploy wiring)
  and 8.2 (final broom - `src-legacy/` is gone, dead code swept) already ran
  earlier in this same layer's unit order.
- The PLAN's own preamble says "CLAUDE.md must be updated when Phase 1 lands (see
  Phase 8 checklist)". The orchestration framework instead slots the actual
  CLAUDE.md update at unit 8.3 (Phase 8 / L8), which is what actually happened on
  this branch - the framework document is authoritative for how this run is
  structured, so this is not a contradiction to resolve, just a note for whoever
  reads the PLAN literally later.
- CLAUDE.md's own "Current scope (locked feature list)" (7 items) does not
  necessarily mean all 7 were built pre-migration - `docs/roadmap.md` is the
  ground truth for what shipped vs. what is still upcoming (R3 habits was "in
  progress next", R4 journal / R5 Google Calendar sync / R6 in-app learning
  generation were not yet built as of roadmap.md's last entry before this plan
  was written). This unit does NOT need to resolve that distinction (that is
  8.4's job, for the parity checklist) - just be aware CLAUDE.md's scope list
  describes the TARGET, not a claim that everything in it already works.
- Icons: per the L2 unit 2.4 story and L5/L7 stories that already reference it,
  `lucide-react` was dropped and replaced by a custom Lynx `<svg>` icon set in
  `src/components/icons` (PLAN 5.9's fallback path). Verify this is really what
  landed (grep, do not assume) before writing the tech-stack table's Icons row -
  if 2.4 actually kept `lucide-react` working through Lynx `<svg>` instead, state
  THAT instead. Either way, state the real outcome - CLAUDE.md's own
  no-placeholder-functions rule extends in spirit to no-placeholder-docs; do not
  write "TBD" or "validate".

IN SCOPE (files):
- `CLAUDE.md` - tech-stack table, "Native iOS & Android (Capacitor)" section,
  repository-layout section (tree diagram + source-folder-layout subsection),
  "UI components - shadcn/ui first" section, the localStorage line in "Auth &
  sessions", the `VITE_PB_URL`/host-resolution line in "Deployment", the
  Capacitor mention in "Do not add without asking".
- `docs/architecture.md` - the "Stack diagram" ASCII block only (see SPEC step
  6); the rest of the file is feature/data-model content unaffected by a client
  framework swap - do not touch it beyond that block unless step 6's grep turns
  up something else genuinely stale.
- `docs/roadmap.md` - grep sweep only; expected outcome is NO changes (see SPEC
  step 7). Do not edit feature/acceptance-criteria prose - that is roadmap
  content, not framework description, and is out of this unit's scope even if it
  looks dated for unrelated reasons.

OUT OF BOUNDS:
- `docs/deploy-google-cloud.md`'s "Native apps against production" /
  `mobile:sync` line - already owned and fixed by unit 8.1 (same layer, runs
  before this unit). Do not duplicate that edit; if you find it still stale when
  you get here, that means 8.1 has not actually landed it yet - note it as a
  cross-unit finding, do not silently fix it yourself and create a merge
  conflict with 8.1's own commit.
- `src/`, `src-legacy/` (gone by now), `Dockerfile`, compose, deploy.yml - no
  code/infra changes, this is a docs-only unit.
- E2E specs / parity checklist (8.4).
- `docs/lynx-run/native-hosts-runbook.md` - created by unit 8.5 (same layer,
  order-independent - see coordination note in SPEC step 4). Do not write that
  file yourself; only add the CLAUDE.md link to it.

BROOM: N/A - this unit edits documentation prose, it does not port or drop code.

SPEC (numbered, concrete):
1. Tech-stack table (CLAUDE.md, currently the table under "## Tech stack
   (locked)"). Replace with the post-migration reality:
   - Build: **Rspeedy** (`@lynx-js/rspeedy`, Rspack-based) + TypeScript.
   - Framework: **ReactLynx** (`@lynx-js/react`, React 17-style API,
     Preact-based) + `@lynx-js/react/compat` for React 18 APIs.
   - Routing: TanStack Router - **Memory routing** (no browser History API) +
     `url-search-params-polyfill`; one route per space + task/program/board
     detail (unchanged route tree shape).
   - Client state: Zustand v5 - one store per domain (unchanged).
   - Server state: TanStack Query v5 + PocketBase SDK, via
     `@lynx-js/react/compat`.
   - Realtime: PocketBase SSE subscriptions via `lynx.EventSource`.
   - Animation: CSS transitions/keyframes (enter/exit) + **Main-Thread
     Scripting** for gesture-linked motion (drag, press-depress, timebox,
     boards).
   - UI primitives: hand-built Lynx primitives (no shadcn/Radix) -
     `src/components/ui/` + the shared tactile primitives in
     `src/components/surface.tsx`.
   - Icons: state the VERIFIED real outcome from CONTEXT above (grep
     `src/components/icons` and `package.json` for `lucide-react` presence
     before writing this row).
   - Styling: Tailwind **v3** + `@lynx-js/tailwind-preset` +
     `rsbuild-plugin-tailwindcss`; the CSS-variable design tokens (`--paper`,
     `--ink`, `--leaf`, `--zest`, `--radius-card`, `.shadow-soft`, ...) are kept.
   - Fonts: Outfit (body) + Fraunces (display) - **static per-weight**
     `@fontsource/{outfit,fraunces}` instances via `@font-face` (variable-axis +
     weight switching unsupported on Lynx).
   - Backend: PocketBase (Go binary) - API, realtime, auth, static web serving
     (unchanged).
   - Auth: PocketBase email + Google OAuth (unchanged).
   - Hosting: One Docker container (PocketBase serves API + built web) on Google
     Compute Engine (unchanged).
   Remove the "Native shells" row entirely (per the PLAN preamble instruction) -
   native platform info moves to its own short section (step 2), not the
   web-tech-stack table.
2. Kill the "## Native iOS & Android (Capacitor)" section. Replace it with a
   short section, e.g.:
   ```
   ## Native (Lynx / Sparkling)

   iOS and Android ship from the same Lynx bundle as web - no separate mobile
   codebase, no WebView. Native hosts are **Sparkling** app shells (TikTok's
   application layer for Lynx) embedding the Lynx engine + bundle, with native
   navigation for drill-in screens.

   Building, signing, and submitting the native hosts requires macOS + Xcode +
   Android Studio, which this repo's automated tooling cannot run - see
   [docs/lynx-run/native-hosts-runbook.md](docs/lynx-run/native-hosts-runbook.md)
   for the full operator manual (Sparkling scaffold, fastlane signing/submit,
   on-device QA checklist, the OTA decision).
   ```
   Keep it short - this is a pointer section, the actual detail lives in the
   runbook (unit 8.5, same layer - see coordination note in step 4).
3. Repository-layout section: in the top-level tree diagram, remove the
   `capacitor.config.ts`, `android/`, `ios/` lines; add `lynx.config.ts`; change
   the `src/` comment from "Vite app" to "ReactLynx app (Rspeedy)". In the
   "Source folder layout" subsection, change the `ui/` line from "shadcn
   primitives" to "hand-built Lynx primitives" (or the verified real wording per
   step 1's Icons finding, if icons also moved under a differently-named folder -
   check `src/components/icons/` still matches "decorative SVGs" or update it).
   The rest of the folder shape (`features/<feature>/`, `lib/`, `stores/`,
   `pages/`, `styles/`) is unchanged per the tree convention ("New-tree layout
   mirrors the old one") - do not restructure prose that is still true.
4. "## UI components - shadcn/ui first" section: replace entirely - shadcn has
   no Lynx equivalent. New text, e.g.:
   ```
   ## UI components - hand-built Lynx primitives first

   Lynx has no shadcn/Radix equivalent. Before building a one-off, check
   `src/components/ui/` and `src/components/surface.tsx` for an existing
   hand-built primitive and extend it with Tailwind classes rather than forking
   it.

   - Lynx primitives live in `src/components/ui/` - hand-built on
     `<view>`/`<text>`/`<input>`, styled via the Lynx Tailwind preset's
     `uiVariants` (Lynx has no pseudo/data-attr selectors, so component states
     route through the preset, not `:hover`/`:focus`/`data-*`).
   - One-off components live in `src/features/<feature>/components/` or
     `src/components/` if genuinely shared.
   - Shared tactile primitives (`Panel`, `Eyebrow`) live in
     `src/components/surface.tsx`.
   ```
   COORDINATION NOTE: this section and its actual file paths must match unit
   2.3's (primitives) real output - grep `src/components/ui/` and
   `src/components/surface.tsx` to confirm they exist with these names before
   committing this wording; adjust names if 2.3 landed something different.
5. "## Auth & sessions" section: the line "PocketBase's auth store persists in
   localStorage" is false post-migration (`localStorage` does not exist on
   Lynx). Grep unit 1.4's PB client seam (`src/lib/pb.ts` or wherever it lives)
   for the actual storage-adapter mechanism it built and reword this line to
   name it accurately (e.g. "persists via the Lynx storage adapter
   (`src/lib/<actual-name>.ts`)") - do not invent a name, use the real one.
6. "## Deployment (Docker -> Google Cloud)" section: the line describing
   `src/lib/pb.ts` resolving the API host via "explicit `VITE_PB_URL` (mobile
   builds) -> dev `127.0.0.1:8090` -> same-origin in production" needs the same
   env-var rename unit 8.1 already made in the Dockerfile (earlier in this same
   layer). Grep the Dockerfile (post-8.1) and `src/lib/pb.ts` for the real name
   and use it here - do not reintroduce `VITE_PB_URL`. If 8.1 found that no
   override hook exists at all (noted as a finding in its own story), reword this
   line to describe whatever the actual resolution order is rather than
   describing a variable that does not exist.
7. "Do not add without asking" line mentions "Capacitor plugins beyond core" -
   replace with the Lynx-appropriate equivalent, e.g. "Sparkling native modules
   beyond what a feature demonstrably needs".
8. `docs/architecture.md`'s "Stack diagram" box-drawing block has a line for
   "React 19 + Vite" and a line for "shadcn/ui . Tailwind v4 . motion/react"
   (open the file and find them - the block uses box-drawing border characters,
   keep those as-is, only change the text inside).
   Replace the framework/styling/animation lines with the Lynx equivalents
   (ReactLynx + Rspeedy; hand-built Lynx primitives + Tailwind v3 preset;
   CSS transitions/keyframes + Main-Thread Scripting), keeping the same
   ASCII-box style and keeping TanStack Router/Query + Zustand as-is (unchanged).
   The rest of `docs/architecture.md` (routes, data model, realtime pattern,
   frontend conventions) describes framework-agnostic behavior the PLAN
   preserves - do not touch it beyond this one block unless your own grep (next
   step) finds something else.
9. Grep `docs/architecture.md` and `docs/roadmap.md` for
   `Vite|Capacitor|shadcn|motion/react|Tailwind v4|@fontsource-variable|
   localStorage|VITE_PB_URL|mobile:sync` (case-insensitive). Expected result:
   `architecture.md` only hits inside the Stack diagram block you just fixed
   (zero hits after your edit); `roadmap.md` is expected to have ZERO hits at all
   (it is feature/acceptance-criteria prose, not a tech-stack description) - if
   your grep finds something in roadmap.md, read the surrounding context and fix
   only that literal stale claim (do not touch unrelated feature-status prose,
   e.g. R3/R4/R5/R6 delivery status is not this unit's concern even if it looks
   out of date - that is a product/roadmap question, not a migration-doc one).

DONE MEANS (mechanical gate):
- `grep -rniE "capacitor|vite|shadcn|tw-animate|@fontsource-variable|
  VITE_PB_URL|mobile:sync" CLAUDE.md docs/architecture.md docs/roadmap.md`
  returns NOTHING (zero matches) - the literal bar stated in this unit's
  assignment.
- CLAUDE.md's tech-stack table, native section, repository layout, and
  UI-components section read accurately against the real committed tree (spot
  check: every file path CLAUDE.md now names actually exists via
  `test -e <path>`).
- `docs/architecture.md`'s Stack diagram block matches the real stack; rest of
  the file untouched (`git diff docs/architecture.md` shows only that block
  changed, unless step 9's grep found something else).
- `docs/roadmap.md` untouched, OR the diff is limited to the exact literal stale
  claim(s) grep found - no broader edits.
- Commit: `lynx(8.3-claude-md-docs): update CLAUDE.md and sweep docs for the
  Lynx stack`.

PARKED (this machine cannot verify; do not fake):
- None - this is a pure documentation unit with a mechanically checkable DONE
  MEANS (grep + path existence). Nothing here requires native hardware or an
  unavailable tool.
