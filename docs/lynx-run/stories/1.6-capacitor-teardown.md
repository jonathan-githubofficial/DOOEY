# 1.6 - Capacitor teardown (branch-only)
LAYER: L1-foundation / TIER: heavy / RISK: medium

GOAL: Remove the Capacitor native shells and dependencies on THIS branch only, so the
web-first Lynx tree carries no Capacitor references in code, while main keeps the
shells (product is never at zero mobile - charter decision 4).

CONTEXT: Units 1.1-1.5 are done: the Lynx web app builds, the client seam works, and
the @l1 E2E gate is green. Capacitor was intentionally left in place until now (PLAN
Phase 1: remove "once Lynx builds, not before"). The old shells wrap the deleted Vite
build and are dead weight on this branch. This unit is branch-only teardown; main is
untouched (we never push/merge here - charter hard rule).

IN SCOPE (files):
  git rm -r android/                 (Capacitor Android shell, generated+committed)
  git rm -r ios/                     (Capacitor iOS shell, generated+committed)
  git rm capacitor.config.ts         (imports @capacitor/cli type)
  EDIT package.json:
    - remove deps: @capacitor/android, @capacitor/core, @capacitor/ios
    - remove devDep: @capacitor/cli
    - remove script: "mobile:sync"  ("npm run build && cap sync")
  npm install                        (updates package-lock.json; removes cap from lock)

OUT OF BOUNDS:
- Do NOT edit CLAUDE.md - its Capacitor sections are unit 8.3's job (charter).
- Do NOT edit docs/* - documentation Capacitor references are handled by the docs
  units; this unit only LISTS them (SPEC 3). DONE MEANS exempts CLAUDE.md and docs.
- Do NOT touch pb/, Dockerfile (it has no Capacitor ref - verified), .github/, or the
  Lynx app source. Do NOT remove lucide-react/motion/fontsource/radix (their own units).

BROOM (what leaves, why):
- android/, ios/: DROP (dead native shells wrapping the removed Vite dist/; native
  Lynx hosts are the Sparkling scaffold, PARKED for this machine - PLAN 6 Phase 8/2c).
- capacitor.config.ts: DROP (Capacitor-only config).
- @capacitor/{core,android,ios,cli}: DROP (PLAN 8 Remove list).
- "mobile:sync" script: DROP (PLAN 8 - it ran `cap sync`).

SPEC (numbered):

1. Remove the shells and config (tracked files a story explicitly brooms - allowed
   deletion per charter):
     git rm -r android ios
     git rm capacitor.config.ts
   These are the only Capacitor-owned code paths (verified by repo grep).

2. Edit package.json: delete the four @capacitor/* dependency lines and the
   "mobile:sync" script line. Then run `npm install` (non-interactive) so
   package-lock.json drops the Capacitor entries. Do NOT hand-edit package-lock.json.

3. Grep the repo for remaining Capacitor references and LIST what each becomes (the
   charter's explicit ask). After steps 1-2, the surviving references (all in
   docs/CLAUDE.md, which are OUT OF BOUNDS here) are:
   - CLAUDE.md "Native iOS & Android (Capacitor)" section + the mobile:sync/cap open
     commands + the repository-layout android/ ios/ entries -> becomes: rewritten to
     the Lynx/Sparkling native-host story (removed Capacitor, no react-native rule
     updated). Owner: unit 8.3 (CLAUDE.md + docs).
   - docs/deploy-google-cloud.md line ~156 `VITE_PB_URL=... npm run mobile:sync`
     -> becomes: the native-build step is now Rspeedy build + Sparkling host + fastlane
     (PLAN 7); the web deploy line stays (web deploy is unchanged). Owner: the deploy
     docs pass (unit 8.3 / docs). LIST only; do not edit here.
   - docs/lynx-migration.md and docs/lynx-run/* Capacitor mentions -> these are the
     migration PLAN and run docs describing the teardown; they are correct as-is,
     leave them.
   - scripts/dev.mjs -> NO Capacitor reference (verified; its Vite->Rspeedy change was
     unit 1.1). Dockerfile -> NO Capacitor reference (verified). Nothing to do.
   Record this list in the unit result so unit 8.3/docs has the exact hit list.

4. Verify no Capacitor reference remains in CODE (everything except CLAUDE.md and
   docs/): grep the repo (excluding node_modules, CLAUDE.md, docs/) for
   `capacitor|@capacitor|cap sync|mobile:sync` -> zero hits.

DONE MEANS (exact commands + expected outcomes):
  1) git status shows android/, ios/, capacitor.config.ts removed; package.json +
     package-lock.json modified.
  2) grep -ri "capacitor" over the repo EXCLUDING node_modules, CLAUDE.md, and docs/
     -> no matches (code is Capacitor-free; docs/CLAUDE.md refs remain for their units).
  3) npm run typecheck -> exits 0.
  4) npm run build     -> exits 0 (web output unaffected by shell removal).
  5) npx playwright test --grep @l1 -> still PASS (the @l1 gate stays green).
  6) main tree / repo root above the worktree untouched (only worktree files changed).
  Commit: "lynx(1.6): remove Capacitor shells + deps (branch-only)".

PARKED (this Windows box):
- Native host packaging (Sparkling iOS/Android) that Capacitor used to provide is out
  of mechanical reach here (no Xcode/Android SDK) and is a PARKED manual runbook
  (charter L8 8.5); removing Capacitor does not regress anything verifiable on web.
