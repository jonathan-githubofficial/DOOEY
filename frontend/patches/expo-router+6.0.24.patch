diff --git a/node_modules/expo-router/build/native-tabs/NativeBottomTabs/NativeTabsView.js b/node_modules/expo-router/build/native-tabs/NativeBottomTabs/NativeTabsView.js
index 106de95..49fb9db 100644
--- a/node_modules/expo-router/build/native-tabs/NativeBottomTabs/NativeTabsView.js
+++ b/node_modules/expo-router/build/native-tabs/NativeBottomTabs/NativeTabsView.js
@@ -160,6 +160,11 @@ function convertOptionsIconToPropsIcon_4_16(icon) {
         return { sfSymbolName: icon.sf };
     }
     else if ('src' in icon && icon.src) {
+        // DOOEY patch: a source marked __keepColor renders as-is (multi-color)
+        // instead of being tinted as a template.
+        if (icon.src.__keepColor) {
+            return { imageSource: icon.src };
+        }
         return { templateSource: icon.src };
     }
     return undefined;
@@ -184,6 +189,20 @@ function convertOptionsIconToPropsIcon_4_18(icon) {
             };
         }
         if ('src' in icon && icon.src) {
+            // DOOEY patch: a source marked __keepColor renders as-is
+            // (multi-color) instead of being tinted as a template.
+            if (icon.src.__keepColor) {
+                return {
+                    // selectedIcon
+                    type: 'imageSource',
+                    imageSource: icon.src,
+                    // icon
+                    ios: {
+                        type: 'imageSource',
+                        imageSource: icon.src,
+                    },
+                };
+            }
             return {
                 // selectedIcon
                 type: 'templateSource',
