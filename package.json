{
  "name": "dooey",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "//": "The app is frontend/ (Expo) and has its own package.json. This root exists only for the learning-program tooling in scripts/, which talks to PocketBase directly.",
  "scripts": {
    "verify-program": "node --disable-warning=MODULE_TYPELESS_PACKAGE_JSON scripts/verify-program.mjs",
    "push-program": "node --disable-warning=MODULE_TYPELESS_PACKAGE_JSON scripts/push-program.mjs"
  },
  "dependencies": {
    "pocketbase": "^0.26.0"
  }
}
